# IRON CROWN v3 온라인 전투·모바일·계정 업데이트

## 실제 원인 분석

### 다른 플레이어가 뚝뚝 끊긴 원인

- 클라이언트가 서버 좌표를 받을 때 다른 플레이어의 표시 좌표로 바로 사용했습니다. `renderX/renderY`와 `targetX/targetY`가 분리되어 있지 않아 서버 패킷 간격이 그대로 순간이동처럼 보였습니다.
- 각 클라이언트는 180ms마다 위치를 보냈고, 서버는 위치 하나를 받을 때마다 전체 플레이어 객체 배열을 모든 접속자에게 전송했습니다. 플레이어가 N명이면 각 클라이언트가 받는 전체 월드 메시지는 약 `5.56 × N회/초`로 증가했습니다.
- 위치 메시지마다 React 상태를 갱신해 게임 월드와 관계없는 UI 렌더도 함께 발생했습니다.

### 보스전 렉이 더 심해진 원인

- 보스 엔진은 20Hz 판정을 수행하면서 모든 지역의 전체 보스 상태를 10Hz로 계속 전송했습니다.
- 플레이어의 보스 공격 한 번마다 별도 `boss:damage` 메시지를 즉시 방송해 공격자가 늘수록 메시지가 함께 증가했습니다.
- 매 프레임 투사체·파티클·보스 효과 배열을 `filter/slice`로 다시 만들었고 화면 밖 객체도 그렸습니다.
- 보스전 종료 및 지역 이동 시 남은 객체 정리는 있었지만 여러 경로에 흩어져 있어 반복 전투에서 불필요한 객체가 오래 남을 여지가 있었습니다.

### 공격력 380이 38로 잘린 원인

서버에 다음과 같은 보스 1회 피해 상한이 있었습니다.

```text
최종 입력 피해 = min(플레이어 피해, 보스 최대 HP × 2.5%)
```

1지역 보스 HP는 1,500이므로 `1,500 × 0.025 = 37.5`, 반올림하면 정확히 38이었습니다. 2지역도 같은 비율 상한 때문에 장비와 강화로 공격력이 커져도 성장분이 사라졌습니다.

### 다른 기기 로그인이 실패한 원인

로그인 화면이 회원가입 전용 `displayName`을 빈 문자열로도 전송했고, 서버는 선택 필드가 전달된 이상 최소 1글자 검증을 적용해 요청을 거절했습니다. 이미 로그인한 기기는 저장 토큰으로 접속해 문제가 드러나지 않고, 새 기기에서 아이디·비밀번호로 로그인할 때만 실패할 수 있었습니다.

## 적용한 구조

| 항목 | v2 | v3 |
|---|---|---|
| 내 캐릭터 | 로컬 즉시 이동 | 그대로 유지 |
| 다른 플레이어 | 서버 좌표 즉시 대입 | 목표 좌표 저장 + delta time 지수 보간 |
| 큰 위치 차이 | 긴 추적 또는 순간 갱신 | 420px 이상, 재접속, 지역 이동은 즉시 스냅 |
| 플레이어 전송 | 5.56Hz 고정 | 이동 중 최대 15Hz, 정지 시 600ms heartbeat |
| 월드 방송 | 입력마다 전역 전체 객체 | 지역별 최대 15Hz compact snapshot, 정적 roster 분리 |
| 보스 판정 | 20Hz | 20Hz 유지 |
| 보스 시각 snapshot | 모든 지역 10Hz | 전투 중인 지역만 8Hz + 16 rate 화면 보간 |
| 보스 공격 | 상태와 피해를 반복 방송 | 패턴 시작 이벤트 즉시, 장식 VFX는 클라이언트 로컬 재생 |
| 보스 피해 표시 | 공격 1회마다 방송 | 최대 12Hz로 묶어서 방송 |
| 렌더 객체 | 매 프레임 배열 재생성 | 배열 제자리 정리, 상한 유지, 화면 밖 그리기 생략 |
| 접속자 정보 | 위치 배열에 정적 정보 반복 | 전체 접속자/지역 roster를 입장·퇴장·장비 변경 시만 전송 |

보스 순간이동 패턴은 일반 보간에서 제외하고 서버 판정 시각에 맞춰 즉시 스냅합니다. 돌진 등 일반 이동만 부드럽게 보간하므로 공격 판정 좌표와 시각 좌표가 오래 어긋나지 않습니다.

## 새 보스 피해 공식

보스 티어별 방어 등급은 10, 14, 18, 22, 26, 30, 34, 38이며 다음 공식을 사용합니다.

```text
방어 적용 피해 = 공격 피해 × 100 ÷ (100 + 보스 방어 등급)
```

이 공식은 방어력을 유지하지만 공격력이 2배가 되면 방어 적용 후 피해도 2배가 됩니다. 따라서 +0부터 +30까지 강화 성장성이 막히지 않으며, 후반 장비로 초반 보스에게 돌아가면 확실히 강해진 결과가 나타납니다. 광산 수호자의 수정 갑옷, 보스별 노출·그로기 추가 배율은 고유 패턴으로 그대로 유지했습니다. 비정상 숫자 안전 검사는 보스 HP의 10배로 두어 실제 처치 결과에는 피해 상한으로 작동하지 않습니다.

공격력 380 예시:

| 대상 | 기존 | v3 기본 방어 적용 |
|---|---:|---:|
| 초원의 뿔짐승 | 38 | 345 |
| 족장 우르크 | 약 100~150 상한 | 333 |

## 협동 보스

- 같은 지역에는 하나의 서버 권위 보스 HP·위치·페이즈·공격 상태만 존재합니다.
- 각 소켓의 공격 제한은 서로 독립적이므로 두 플레이어의 동시 공격이 덮어써지지 않습니다.
- 기여량은 계정별로 누적합니다. 같은 계정 중복 보상은 막고, 실제 공격한 서로 다른 플레이어는 모두 XP·골드·확정 장비 판정을 각각 한 번 받습니다.
- 타겟은 여러 플레이어 중 선택하며 현재 타겟 사망·이탈·접속 종료 시 즉시 다시 선택합니다. 정상 상태에서는 3.2~5초 주기로만 재평가합니다.

## 휴대폰 가로 UI

- 높이 500px 이하 가로 화면을 width와 별개인 전용 breakpoint로 처리합니다.
- 로그인 카드는 왼쪽 로고/설명, 오른쪽 로그인 폼의 2열 구조가 되며 `100dvh`와 fallback을 함께 사용합니다.
- 상단 로고는 왕관만 남기고 레벨·골드·접속자·소리·전체화면·로그아웃을 한 줄에 유지합니다.
- 접속자 버튼은 숨기지 않으며 누르면 전체 접속자의 닉네임·레벨·칭호·지역·무기·HP가 열립니다.
- HP/XP는 항상 보이고 무기 정보는 760px 이하에서만 접어 전투 공간을 확보합니다.
- 미니맵은 100×62px, 지역명은 작은 별도 배지로 표시합니다.
- 조이스틱과 공격·대쉬·방어·포션·전설 스킬은 엄지 크기를 유지하고 PC 키 표시는 숨깁니다.
- 인벤토리·지역·도감·도움말은 삭제하지 않고 작은 아이콘 메뉴로 유지합니다.
- 태블릿 1024×768 이상은 기존 레이아웃을 유지합니다.

## 검증 결과

### 자동·통합 테스트

- 단위/구조/협동 테스트: 23개 통과
- TypeScript + Vite production build: 통과
- 실제 메모리 PostgreSQL + API + Socket.IO 통합 테스트: 통과
- 계정 2개 생성, 저장 위조 방어, 일반 처치, 상점, 강화, 이전 클라이언트 형식 로그인, 재접속 저장 복원: 통과
- 두 플레이어 동시 보스 공격: 서버에서 9타/1,638 피해 승인, 패턴·페이즈 전환 뒤 두 계정 모두 보상 1회 수령
- 보스 사망 시 공격 객체 제거 및 보상 1회 처리: 통과
- +0/+5/+10/+15/+20/+25/+30 및 8개 지역 대표 무기의 피해 증가: 모두 단조 증가

통합 테스트 4.90초 동안 서버 계측값:

| 지표 | 결과 |
|---|---:|
| 플레이어 presence 수신 | 76 |
| 서버 world snapshot | 37 |
| 평균 world snapshot | 103 bytes |
| 보스 state snapshot | 16 |
| 보스 pattern 이벤트 | 1 |
| 묶음 damage 방송 | 4 |
| 해결된 공격 판정 객체 | 1 |

2인 예시 payload는 기존 전체 객체 배열 약 533 bytes에서 compact snapshot 약 105 bytes로 약 80% 감소했습니다. 이는 같은 샘플 데이터를 직렬화한 비교이며, 기존 배포 서버의 실측 로그는 아닙니다.

요청된 740×360, 800×360, 844×390, 915×412, 1024×768, 1280×800, 1366×768, 1920×1080 분기 조건을 자동 검증했습니다. 현재 실행 환경에는 실제 모바일 브라우저 렌더러가 없어 픽셀 스크린샷 비교 대신 breakpoint·필수 UI 노출·production build를 검증했습니다. 배포 뒤 Galaxy Chrome에서 주소창이 열린 상태와 전체화면 상태를 각각 한 번 확인하는 것이 마지막 실제 기기 점검입니다.

## 변경하지 않은 것

보스 패턴 수·공격력·HP·XP·드랍, 몬스터 밸런스, 플레이어 이동속도·대쉬, 무기 공격속도, 강화 확률·비용, 포션, 지역, 장비, 저장 데이터 구조는 변경하지 않았습니다.
