# IRON CROWN v4 업데이트 보고서

## 실제 병목과 지연 원인

- 보스 판정은 서버의 절대 시각으로 처리했지만, 클라이언트는 `boss:pattern` 패킷을 받은 즉시 소리를 재생하고 보스 몸체는 별도 snapshot 보간으로 움직였습니다. 네트워크 지연이 커질수록 예고 이펙트·소리·몸체 움직임의 시작 프레임이 서로 달라졌습니다.
- 보스 위치 snapshot은 8Hz라서 일반 이동은 보간되더라도 목표 좌표 갱신 간격이 최대 125ms였습니다. 공격 이동은 pattern 이벤트가 아니라 이 snapshot을 뒤따라가 체감 지연이 더 컸습니다.
- 투기장 봉인은 보스의 전역 `engaged` 값을 그대로 사용했습니다. 한 명이 전투를 시작하면 같은 지역의 모든 클라이언트에서 벽과 충돌이 생겨, 참가하지 않은 플레이어도 갇히거나 중도 참가가 불명확했습니다.
- 참가자가 모두 사라졌을 때 공격 객체는 지웠지만 전투 기여·페이즈·체력 회복을 하나의 원자적인 전멸 처리로 묶지 않았습니다.
- JWT가 서명과 만료 시간만 확인하는 구조라 새 기기에서 로그인해도 이전 기기의 토큰이 30일 동안 계속 유효했습니다.
- 모바일은 HUD만 작아지고 월드 시야가 PC와 같은 1:1 배율이라 좁은 화면에서 보스 예고 범위를 파악하기 어려웠습니다.

## 적용한 구조

| 항목 | v3 | v4 |
|---|---|---|
| 보스 판정 tick | 20Hz | 20Hz 유지 |
| 보스 위치 snapshot | 8Hz + 보간 | 12Hz + delta time 보간 |
| 공격 동기화 | pattern 효과와 snapshot 위치가 분리 | `attackStartAt/warnAt/resolveAt/serverNow` 공통 타임라인 |
| 공격 이동 | 다음 snapshot을 추적 | 이벤트의 시작·도착 좌표로 동일 시간축에서 재생 |
| 효과·소리 | 패킷 처리 순간 각각 시작 | 첫 렌더 프레임에 함께 시작 |
| 투기장 봉인 | 지역 전체 `engaged` | 서버 참가자 Set + 내 참가 상태만 벽/충돌 적용 |
| 중도 참가 | 전역 상태에 종속 | 투기장 진입 시 기존 전투에 개별 참가 |
| 전멸 | 공격 정리만 수행 | 750ms 확인 후 1회 초기화, 최대 HP 50% 회복, 귀환·타겟·공격·기여 초기화 |
| 로그인 | 만료 전 JWT 모두 유효 | DB `active_session_id`와 일치하는 최신 세션 하나만 유효 |
| 모바일 시야 | 월드 1.0배 | 월드만 0.8배, DOM HUD는 1.0배 유지 |

서버는 보스 공격마다 시작 시각, 예고 시각, 판정 시각, 보스 시작 좌표, 목표 좌표, 방향과 이동 종류를 한 번 전송합니다. 클라이언트는 서버-클라이언트 시계 차이를 완만하게 보정한 뒤 동일한 현지 시간축으로 변환합니다. 순간이동은 판정 시각에 스냅하고 돌진·도약은 시간 기반 easing으로 표시하므로 프레임률과 snapshot 도착 간격에 종속되지 않습니다. 피해 판정은 계속 서버 좌표와 서버 시각을 사용합니다.

모바일 효과는 패턴 개수나 외형을 줄이지 않았습니다. 화면 밖 효과를 계속 제외하고, 매 호출마다 만들던 팔레트 객체를 정적으로 재사용하며 모바일에서 shadow blur 비용만 낮췄습니다. 파티클과 보스 효과의 기존 상한·제자리 배열 정리도 유지했습니다.

## 협동 전투와 전멸 규칙

- 같은 지역의 보스 HP·위치·페이즈·판정은 하나이며, 참가자만 공격과 피해 판정 대상입니다.
- 봉인벽 표시와 입구 충돌은 각 클라이언트의 참가 승인 상태만 사용합니다. 관전자는 벽이 없고, 안으로 들어오면 즉시 참가 요청을 보내 기존 전투에 합류합니다.
- 마지막 생존 참가자가 사망·재설정·지역 이동·접속 종료로 사라지면 750ms 유예 후 한 번만 초기화합니다.
- 초기화는 최대 HP의 50%만큼 회복하되 최대 HP를 넘지 않으며, 모든 진행 중 공격과 효과·타겟·기여 기록을 취소하고 보스를 홈 위치로 돌려보냅니다. 처치가 아니므로 보상은 없습니다.
- 재설정 버튼은 별도 순간이동 기능이 아니라 기존 사망 처리에 매우 큰 피해를 전달합니다. 따라서 2.4초 부활, 입구 귀환, 보스 참가 이탈과 전멸 판정이 모두 같은 경로를 사용합니다.

## 계정 세션

회원가입과 로그인 때 임의의 세션 ID를 발급하고 PostgreSQL 사용자 행에 최신 값 하나만 저장합니다. 모든 HTTP 인증과 Socket.IO 연결은 JWT의 세션 ID가 DB 값과 같은지 검사합니다. 새 로그인은 DB 값을 먼저 교체한 뒤 이전 소켓에 `session:replaced`를 보내고 연결을 닫습니다. 이전 탭을 새로고침해도 이전 토큰은 `401 SESSION_REPLACED`로 거절됩니다. 저장 데이터 형식과 계정 진행도는 바꾸지 않았습니다.

## 카메라와 좌표 보정

- 휴대폰·가로 높이 500px 이하 화면의 월드 배율은 0.8입니다.
- 카메라 clamp와 화면 밖 culling은 `화면 크기 ÷ zoom`으로 계산합니다.
- 포인터 좌표는 `화면 좌표 ÷ zoom + camera`로 역변환합니다.
- 화면 흔들림은 월드 scale 전에 적용해 기기별 체감 픽셀 크기를 유지합니다.
- 미니맵과 React HUD는 world transform 복원 뒤 그리므로 확대/축소되지 않습니다.

## 검증 범위

- 일반 이동·일반 몬스터 전투의 로컬 입력 경로와 밸런스는 변경하지 않았습니다.
- 2인 보스 동시 공격·각 계정 보상, 참가자/관전자 봉인 분리, pattern 타임라인 필드, 마지막 참가자 이탈 후 1회 전멸 초기화를 자동 검증합니다.
- 모바일 390×844, 844×390, 740×360과 PC 1366×768의 카메라 배율 분기를 검증합니다.
- 새 로그인 후 이전 토큰의 API 접근이 `SESSION_REPLACED`로 거절되는지 통합 검증합니다.
- TypeScript, Vite production build, 메모리 PostgreSQL, 실제 HTTP와 Socket.IO 경로를 함께 검사합니다.

최종 결과는 자동 테스트 26개와 client/server production build가 모두 통과했습니다. 2인 통합 보스전에서 서버는 presence 74회, 지역 world snapshot 37회, 평균 snapshot 103 bytes, 보스 snapshot 24회, 묶음 피해 방송 4회, 플레이어 공격 9회/1,638 피해를 처리했고 두 계정 모두 보상을 한 번씩 받았습니다. 이 실행은 약 4.8초의 짧은 기능 검증 샘플이며 장기 부하 시험 수치가 아닙니다.

v3 대비 플레이어 위치 전송은 최대 15Hz, world snapshot은 최대 15Hz, 보스 판정은 20Hz로 그대로입니다. 보스 시각 snapshot만 8Hz에서 12Hz로 올려 최악의 목표 갱신 간격을 125ms에서 약 83ms로 줄였습니다. 공격 시작은 snapshot을 기다리지 않고 단일 pattern 이벤트로 즉시 전송하므로 패턴 복잡도에 따라 전체 보스 상태를 매 tick 반복 전송하지 않습니다.

실제 단말의 GPU FPS와 셀룰러 지연은 이 빌드 환경에서 직접 계측할 수 없으므로 숫자를 꾸며내지 않았습니다. 배포 후 Android Chrome과 iPhone Safari에서 후반 페이즈 2인 반복 전투를 각각 확인하는 것이 마지막 실기기 확인 항목입니다.

## 변경하지 않은 것

보스 패턴 수·공격력·HP·XP·드랍, 일반 몬스터 밸런스, 플레이어 이동속도·대쉬, 무기 공격속도, 강화·포션·지역·장비·저장 데이터는 변경하지 않았습니다.
